import { StatusBar } from "expo-status-bar";
import { StyleSheet, Text, View, TextInput } from "react-native";
import { Image } from "react-native"
import { Link } from "expo-router"

export default function DisasterCardsContainer() {
    return (<>
    
    </>);
}